seeds:
  - name: city_region
    config:
      column_types:
        city: varchar(75)
        region: varchar(75)
        updated_at: timestamp
    columns:
      - name: city
        tests:
          - not_null
          - unique